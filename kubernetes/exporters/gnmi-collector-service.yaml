apiVersion: v1
kind: Service
metadata:
  name: st2110-gnmi-collector
  namespace: st2110-monitoring
spec:
  selector:
    app: st2110-gnmi-collector
  ports:
  - port: 9273
    targetPort: 9273
    name: metrics
  type: ClusterIP

