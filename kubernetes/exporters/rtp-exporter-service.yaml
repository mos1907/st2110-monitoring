apiVersion: v1
kind: Service
metadata:
  name: st2110-rtp-exporter
  namespace: st2110-monitoring
spec:
  selector:
    app: st2110-rtp-exporter
  ports:
  - port: 9100
    targetPort: 9100
    name: metrics
  type: ClusterIP

